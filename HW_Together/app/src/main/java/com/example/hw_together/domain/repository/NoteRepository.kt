package com.example.hw_together.domain.repository

import com.example.hw_together.domain.models.Note
import retrofit2.Response

interface NoteRepository {
    //suspend fun saveNote(note: Note): Response<>
}